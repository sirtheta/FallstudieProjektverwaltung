<UserControl x:Class="Projektmanagement.Views.NewProcessModelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:local="clr-namespace:Projektmanagement.Views"
         mc:Ignorable="d" 
             Background="PapayaWhip"
         d:DesignHeight="300" 
         d:DesignWidth="300">
  <Grid>
    <ScrollViewer>
      <StackPanel>
        <Button Content="Add New" Command="{Binding BtnAddNew}" Margin="10" HorizontalAlignment="Right"/>
        <ItemsControl ItemsSource="{Binding ItemCollection}">
          <ItemsControl.ItemTemplate>
            <DataTemplate>
              <TextBox  materialDesign:HintAssist.Hint="Input Phase Name"/>
            </DataTemplate>
          </ItemsControl.ItemTemplate>
        </ItemsControl>
        <Button Content="Save" Command="{Binding BtnSave}" Margin="10" HorizontalAlignment="Right" VerticalAlignment="Bottom"/>
      </StackPanel>
    </ScrollViewer>
  </Grid>
</UserControl>